# Install OS on Pi
# Execute this script for Setup

# Update
sudo apt-get update && audo apt-get upgrade

# Load dir and dependencies
git clone https://github.com/ldroll/io-ml-test
cd io-ml-test

# Install libraries (tflite env)
sudo pip3 install virtualenv
python3 -m venv ml-test-env
source ml-test-env/bin/activate
python3 -m pip install tflite-runtime

# Start Main
# Main is fed with input
